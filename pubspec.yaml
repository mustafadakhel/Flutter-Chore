name: flutter_chore
description: A flutter plugin for invoking operations once or only a specific amount of times
version: 0.0.2
homepage: https://github.com/mustafadakhel/Flutter-Chore

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter