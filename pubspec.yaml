name: flutter_boot
description: A flutter plugin for executing operations only once
version: 0.0.1
homepage:

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: 2.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
